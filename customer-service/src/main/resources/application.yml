server:
   port: 9090
   
spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: flipkartCustomer

 
  application:
    name: CUSTOMER-SERVICE
 
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost 

jwt:
  secret: secretkey123

data:
  exception:
    msg: Customer found already.....!!     