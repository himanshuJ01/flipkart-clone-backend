server:
   port: 9091
   
spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: flipkartProduct
#      uri: mongodb+srv://admin:h123@cluster0.z7qznjc.mongodb.net/flipkartProduct?retryWrites=true&w=majority
 
  application:
    name: PRODUCT-SERVICE

 
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost

data:
  exception:
    msg: Product found already.....!!         